# ParentChild-Linker // Multi-Granularity Document Fragment Map

A structural chunk map implementation linking fine-grained vector shards to structural parent documents inside high-speed lookup tables.

## Execution
```bash
python parent_child_rag.py
